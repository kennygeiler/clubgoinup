<% if current_user.promotors == nil %>
  <% redirect_to new_quote_path %>
<% else %>
  <% @quotes.each do |quote| %>
    <%= link_to quote.user.name, quote_path(quote.id) %>
    <%= quote.date %>
    <%= quote.venues %>
  <% end %>
<% end %>